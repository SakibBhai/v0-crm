import { Target, Clock, AlertCircle, CheckCircle, Pause } from "lucide-react"

// Ensure we have a Project type exported from somewhere, but wait, the project page defined the Project interface directly inside!
// Let's create the Project interface here, or copy it from projects/page.tsx if it doesn't exist in lib/types.
